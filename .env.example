# Optional — all have sensible defaults. Export before `open-refinery serve`.
# PORT=8000
# DATABASE_URL=sqlite:///open-refinery.db
# LOG_LEVEL=INFO

# Coming with OAuth / token encryption: SECRET_KEY (generate with
# python -c "import secrets; print(secrets.token_urlsafe(32))")
